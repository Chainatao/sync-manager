╔═══════════════════════════════════════════════════════════════════════════╗
║                                                                           ║
║               🎯 SYNC MANAGER - QUICK REFERENCE CARD 🎯                  ║
║                                                                           ║
╚═══════════════════════════════════════════════════════════════════════════╝


┌───────────────────────────────────────────────────────────────────────┐
│ 📋 BASIC WORKFLOW                                                     │
└───────────────────────────────────────────────────────────────────────┘

  SOURCES              SYNC MANAGER            TARGETS
  (Input)              (Brain)                 (Output)
     │                      │                      │
     │  1. Import Data      │                      │
     ├─────────────────────>│                      │
     │                      │                      │
     │                      │  2. Process &        │
     │                      │     Track Changes    │
     │                      │                      │
     │                      │  3. Sync Data        │
     │                      ├─────────────────────>│
     │                      │                      │
     │                      │  4. Detect Manual    │
     │                      │<────────────────────┤│
     │                      │     Changes          │


┌───────────────────────────────────────────────────────────────────────┐
│ 🚀 INITIAL SETUP (One Time)                                          │
└───────────────────────────────────────────────────────────────────────┘

  1. Initialize Database
     ─────────────────────────────────────────────────────────────────
     python -m sync_manager.cli init


  2. Create Tenant
     ─────────────────────────────────────────────────────────────────
     python -m sync_manager.cli create-tenant "My Company"
     
     📝 SAVE THE API KEY!


  3. Start API Server
     ─────────────────────────────────────────────────────────────────
     uvicorn sync_manager.api:app --reload
     
     Visit: http://localhost:8000/docs


┌───────────────────────────────────────────────────────────────────────┐
│ 📊 DAILY OPERATIONS                                                   │
└───────────────────────────────────────────────────────────────────────┘

  Import New Data
  ───────────────────────────────────────────────────────────────────
  python -m sync_manager.cli import TENANT_ID SOURCE_ID data.json
  
  Example:
  python -m sync_manager.cli import 1 1 properties.json


  Sync to Target
  ───────────────────────────────────────────────────────────────────
  python -m sync_manager.cli sync TENANT_ID SOURCE_ID TARGET_ID
  
  Example:
  python -m sync_manager.cli sync 1 1 1


  Cleanup Old Data
  ───────────────────────────────────────────────────────────────────
  python -m sync_manager.cli cleanup TENANT_ID --days 30
  
  Example:
  python -m sync_manager.cli cleanup 1 --days 30


┌───────────────────────────────────────────────────────────────────────┐
│ 🌐 API EXAMPLES                                                       │
└───────────────────────────────────────────────────────────────────────┘

  All requests need header:
  ───────────────────────────────────────────────────────────────────
  -H "X-API-Key: your_api_key_here"


  Create Source
  ───────────────────────────────────────────────────────────────────
  curl -X POST http://localhost:8000/sources \
    -H "X-API-Key: YOUR_KEY" \
    -H "Content-Type: application/json" \
    -d '{"name": "Properties", "type": "json", "config": {}}'


  Create Target
  ───────────────────────────────────────────────────────────────────
  curl -X POST http://localhost:8000/targets \
    -H "X-API-Key: YOUR_KEY" \
    -H "Content-Type: application/json" \
    -d '{"name": "Website", "type": "website", "config": {}}'


  Import Data
  ───────────────────────────────────────────────────────────────────
  curl -X POST http://localhost:8000/sources/1/import \
    -H "X-API-Key: YOUR_KEY" \
    -H "Content-Type: application/json" \
    -d '{"data": [{"id": "1", "title": "Property"}]}'


  Sync
  ───────────────────────────────────────────────────────────────────
  curl -X POST http://localhost:8000/sync/1/1 \
    -H "X-API-Key: YOUR_KEY"


  List Properties
  ───────────────────────────────────────────────────────────────────
  curl http://localhost:8000/sources/1/properties \
    -H "X-API-Key: YOUR_KEY"


┌───────────────────────────────────────────────────────────────────────┐
│ 💡 KEY CONCEPTS                                                       │
└───────────────────────────────────────────────────────────────────────┘

  Tenant
  ───────────────────────────────────────────────────────────────────
  Your workspace. Has API key. Isolates your data.


  Source
  ───────────────────────────────────────────────────────────────────
  Where data COMES FROM:
  • JSON files
  • Web scraping
  • Manual entry
  • External APIs


  Target
  ───────────────────────────────────────────────────────────────────
  Where data GOES TO:
  • Your website
  • Database
  • API endpoints
  • Other systems


  Property
  ───────────────────────────────────────────────────────────────────
  Individual data record with:
  • external_id (unique identifier)
  • data (JSON object)
  • hash (for change detection)


  Snapshot
  ───────────────────────────────────────────────────────────────────
  Historical version before update
  • Kept for 30 days
  • Used for rollback
  • Audit trail


┌───────────────────────────────────────────────────────────────────────┐
│ 🎨 EXAMPLE: REAL ESTATE WORKFLOW                                     │
└───────────────────────────────────────────────────────────────────────┘

  Step 1: Setup (Once)
  ───────────────────────────────────────────────────────────────────
  python -m sync_manager.cli init
  python -m sync_manager.cli create-tenant "My Agency"
  
  # Save API key: abc123...
  
  # Create source via API
  curl -X POST http://localhost:8000/sources \
    -H "X-API-Key: abc123..." \
    -d '{"name": "YM Listings", "type": "scraped"}'
  
  # Create target via API
  curl -X POST http://localhost:8000/targets \
    -H "X-API-Key: abc123..." \
    -d '{"name": "My Website", "type": "website"}'


  Step 2: Daily Import (Automated)
  ───────────────────────────────────────────────────────────────────
  # Scrape YM (your existing script)
  python scrape_ym.py > ym_properties.json
  
  # Import to Sync Manager
  python -m sync_manager.cli import 1 1 ym_properties.json
  
  # Result: Created: 50, Updated: 10


  Step 3: Sync to Website
  ───────────────────────────────────────────────────────────────────
  python -m sync_manager.cli sync 1 1 1
  
  # Result: Created: 50, Updated: 10, Warnings: 2


  Step 4: Check Warnings
  ───────────────────────────────────────────────────────────────────
  curl http://localhost:8000/targets/1/properties \
    -H "X-API-Key: abc123..." | jq '.[] | select(.has_manual_changes)'
  
  # Shows properties with manual edits


  Step 5: Monthly Cleanup
  ───────────────────────────────────────────────────────────────────
  python -m sync_manager.cli cleanup 1 --days 30


┌───────────────────────────────────────────────────────────────────────┐
│ 🔍 MONITORING & DEBUGGING                                             │
└───────────────────────────────────────────────────────────────────────┘

  Check Health
  ───────────────────────────────────────────────────────────────────
  curl http://localhost:8000/health


  List All Sources
  ───────────────────────────────────────────────────────────────────
  curl http://localhost:8000/sources -H "X-API-Key: YOUR_KEY"


  List All Targets
  ───────────────────────────────────────────────────────────────────
  curl http://localhost:8000/targets -H "X-API-Key: YOUR_KEY"


  Check Properties in Source
  ───────────────────────────────────────────────────────────────────
  curl http://localhost:8000/sources/1/properties \
    -H "X-API-Key: YOUR_KEY"


  Check Properties in Target (with warnings)
  ───────────────────────────────────────────────────────────────────
  curl http://localhost:8000/targets/1/properties \
    -H "X-API-Key: YOUR_KEY"


  Interactive API Docs
  ───────────────────────────────────────────────────────────────────
  http://localhost:8000/docs


┌───────────────────────────────────────────────────────────────────────┐
│ ⚠️  COMMON ISSUES & SOLUTIONS                                         │
└───────────────────────────────────────────────────────────────────────┘

  Import returns 0 created
  ───────────────────────────────────────────────────────────────────
  ✓ Ensure JSON has "id" or "external_id" field
  ✓ Data must be array: [{"id": "1", ...}]
  ✓ Check IDs are strings or numbers, not null


  Manual changes not detected
  ───────────────────────────────────────────────────────────────────
  ✓ Property must be synced from source first
  ✓ Check source_property_id is set
  ✓ Hash must be different


  API returns 401 Unauthorized
  ───────────────────────────────────────────────────────────────────
  ✓ Check API key is correct
  ✓ Verify header: X-API-Key (not Authorization)
  ✓ No spaces in API key


  Database connection error
  ───────────────────────────────────────────────────────────────────
  ✓ PostgreSQL must be running
  ✓ Check .env DATABASE_URL
  ✓ Test: psql -U postgres


┌───────────────────────────────────────────────────────────────────────┐
│ 📝 JSON FORMAT EXAMPLE                                                │
└───────────────────────────────────────────────────────────────────────┘

  properties.json:
  ───────────────────────────────────────────────────────────────────
  [
    {
      "id": "PROP001",
      "title": "Beautiful Villa",
      "price": 500000,
      "bedrooms": 4,
      "location": "Madrid",
      "description": "Spacious villa with pool"
    },
    {
      "id": "PROP002",
      "title": "City Apartment",
      "price": 250000,
      "bedrooms": 2,
      "location": "Barcelona",
      "description": "Modern apartment downtown"
    }
  ]
  
  Required:
  • "id" or "external_id" field (unique identifier)
  • Must be JSON array
  
  Optional:
  • Any other fields as needed


┌───────────────────────────────────────────────────────────────────────┐
│ 🎯 AUTOMATION EXAMPLE                                                 │
└───────────────────────────────────────────────────────────────────────┘

  daily_sync.py:
  ───────────────────────────────────────────────────────────────────
  import requests
  import json
  
  API = "http://localhost:8000"
  KEY = "your_api_key"
  
  # 1. Load data
  data = json.load(open("properties.json"))
  
  # 2. Import
  r = requests.post(f"{API}/sources/1/import",
      headers={"X-API-Key": KEY},
      json={"data": data})
  print(f"Import: {r.json()['stats']}")
  
  # 3. Sync
  r = requests.post(f"{API}/sync/1/1",
      headers={"X-API-Key": KEY})
  print(f"Sync: {r.json()['stats']}")
  
  
  Schedule with Windows Task Scheduler:
  ───────────────────────────────────────────────────────────────────
  • Run: python daily_sync.py
  • Trigger: Daily at 3:00 AM
  • Run whether user is logged in or not


┌───────────────────────────────────────────────────────────────────────┐
│ 📚 DOCUMENTATION                                                      │
└───────────────────────────────────────────────────────────────────────┘

  • SYNC_MANAGER_USAGE_GUIDE.md  - Complete guide (you are here!)
  • IMPLEMENTATION.md             - Technical architecture
  • QUICKSTART.md                 - 5-minute quickstart
  • http://localhost:8000/docs    - Interactive API docs


┌───────────────────────────────────────────────────────────────────────┐
│ 🎊 GETTING HELP                                                       │
└───────────────────────────────────────────────────────────────────────┘

  1. Check API docs: http://localhost:8000/docs
  2. Read SYNC_MANAGER_USAGE_GUIDE.md
  3. Review examples above
  4. Test with small dataset first


═══════════════════════════════════════════════════════════════════════


           Ready to sync! Start with: python -m sync_manager.cli init


═══════════════════════════════════════════════════════════════════════
